/**
 * Conventional Commits, enforced by the commit-msg git hook.
 * e.g. `feat(quote): add margin-floor guard`, `fix(booking): …`, `docs: …`.
 */
export default {
  extends: ['@commitlint/config-conventional'],
};
